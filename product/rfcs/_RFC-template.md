# RFC-XXX：{標題}

> 把這份檔案複製為 `RFC-XXX-{kebab-case-title}.md` 然後填寫。XXX 為 3 位數遞增。

| 欄位 | 內容 |
|------|------|
| **狀態** | Draft / Accepted / Implemented / Superseded by RFC-XXX |
| **建立日期** | YYYY-MM-DD |
| **最後更新** | YYYY-MM-DD |
| **作者** | |
| **對應版本** | v1.x |
| **CHANGELOG entry** | [v1.x](../../CHANGELOG.md#v1x) |
| **PRD 章節** | [PRD § 章節](../PRD.md#章節) |
| **相關 commit** | `commit-sha` |

---

## 1. Summary

{1-2 段，講清楚這個 RFC 在做什麼以及為什麼。讀者讀完這段就應該知道是否需要繼續讀。}

## 2. Background / Problem

### 觀察到的問題

{什麼狀況觸發了這個變更？引用案例、引用 issue、引用對話。具體比抽象好。}

### 現況的限制

{在做這個 RFC 之前，系統如何處理這個問題？為什麼不夠？}

## 3. Goals / Non-goals

### Goals

- {要解決的具體問題 1}
- {要解決的具體問題 2}

### Non-goals

- {刻意不在這個 RFC 範圍內的東西}
- {即使技術上可以順便做、但我們選擇不做的事}

## 4. Options Considered

至少列出 2-3 個被認真考慮過的選項。**如果只有一個選項，這份 RFC 不夠成熟。**

### Option A: {名稱}

{描述}

**優點**：
**缺點**：

### Option B: {名稱}

{描述}

**優點**：
**缺點**：

### Option C: {名稱}

{描述}

**優點**：
**缺點**：

## 5. Decision

**選擇**：Option {X}

**理由**：{為什麼選這個而不是其他。要明確指出取捨——什麼被犧牲了、為什麼可以接受。}

### 關鍵決策點

{列出做這個 RFC 過程中的次級決策。例：「目錄 slug 用中文原樣 vs 羅馬化」「MANIFEST 強制 vs 選用」}

## 6. 效能影響

### 設計時預估

| 維度 | 變更前 | 變更後 | Δ |
|------|--------|--------|---|
| 預載 token（系統 prompt + skill + agent） | | | |
| 平均階段新增 Read 呼叫 | | | |
| 平均階段新增 Glob 呼叫 | | | |
| 預期搜尋次數變化 | | | |
| 預期 fetch 次數變化 | | | |
| 命中率預期變化 | | | |
| 證據等級分布預期變化 | | | |

> **命中率定義**：被報告引用 / 直接導致下一步驗證的搜尋呼叫數 ÷ 總搜尋呼叫數。完整定義見 [`product/perf/README.md`](../perf/README.md)。

### 實測結果

> 首次跑真實案例後回填。如果這個變更不需要實測（純文件、純註解），寫「N/A — 無需實測」並說明理由。

| 維度 | 預估 | 實測 | 偏差 | 備註 |
|------|------|------|------|------|
| | | | | |

**測試案例**：{哪一家公司、哪個路徑、哪個規模分類}
**測試日期**：YYYY-MM-DD
**對照 baseline**：[baseline-vX.X-XXX.md](../perf/baseline-v1.1-token-usage.md)

## 7. 風險

| 風險 | 嚴重度 | 緩解方式 |
|------|--------|---------|
| | | |

## 8. Implementation Plan

### 需要新增的檔案

- `path/to/file` — {用途}

### 需要修改的檔案

- `path/to/file` — {改了什麼}

### 不動的檔案

- {可能會以為要改但其實不用改的，避免 review 浪費時間}

## 9. Followups / Open Questions

- {實作後發現的問題}
- {留給下一個 RFC 解的東西}
- {不確定的假設，實測後驗證}

## 10. References

- {相關文件、外部資源、對話節錄}
